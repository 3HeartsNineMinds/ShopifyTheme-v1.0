<section class="section cart-section">
  <div class="page-width">
    <div class="cart-header">
      <div>
        <p class="section-kicker">Your selection</p>
        <h1 class="section-title">Basket</h1>
      </div>
      <a class="button button--secondary" href="{{ routes.root_url }}">Continue shopping</a>
    </div>

    {% if cart.item_count > 0 %}
      <form action="{{ routes.cart_url }}" method="post" class="cart-form">
        <table class="cart-table">
          <thead>
            <tr>
              <th>Product</th>
              <th>Size</th>
              <th>Quantity</th>
              <th>Total</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {% for item in cart.items %}
              <tr class="cart-row">
                <td class="cart-cell cart-cell--product">
                  <div class="cart-product">
                    {% if item.image %}
                      {{ item.image | image_url: width: 180 | image_tag: class: 'cart-product__image', loading: 'lazy' }}
                    {% endif %}
                    <div>
                      <strong>{{ item.product.title }}</strong>
                      <br><small>{{ item.final_price | money }} each</small>
                    </div>
                  </div>
                </td>
                <td class="cart-cell cart-cell--size" data-label="Size">{% unless item.variant.title == 'Default Title' %}{{ item.variant.title }}{% else %}&mdash;{% endunless %}</td>
                <td class="cart-cell cart-cell--qty" data-label="Qty"><input class="input cart-quantity" type="number" name="updates[]" value="{{ item.quantity }}" min="0" aria-label="Quantity for {{ item.product.title | escape }}"></td>
                <td class="cart-cell cart-cell--total" data-label="Total">{{ item.final_line_price | money }}</td>
                <td class="cart-cell cart-cell--remove"><a class="remove-link" href="{{ item.url_to_remove }}" aria-label="Remove {{ item.product.title | escape }} from basket">Remove</a></td>
              </tr>
            {% endfor %}
          </tbody>
        </table>

        <div class="cart-summary">
          <p class="cart-summary__label">Subtotal</p>
          <p class="cart-summary__price">{{ cart.total_price | money }}</p>
          <p class="cart-summary__note">Shipping and discount codes are calculated at checkout.</p>
          <div class="product-actions">
            <button class="button button--secondary" type="submit" name="update">Update basket</button>
            <button class="button" type="submit" name="checkout">Checkout</button>
          </div>
        </div>
      </form>
    {% else %}
      <div class="empty-cart-panel">
        <p>Your basket is empty.</p>
        <a class="button" href="{{ routes.root_url }}">Continue shopping</a>
      </div>
    {% endif %}
  </div>
</section>

{% schema %}
{ "name": "Main basket", "settings": [] }
{% endschema %}
