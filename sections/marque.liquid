<div class="marquee" aria-label="Nine Minds message">
  <div class="marquee__track">
    <span>{{ section.settings.text }}</span>
    <span>{{ section.settings.text }}</span>
    <span>{{ section.settings.text }}</span>
    <span>{{ section.settings.text }}</span>
  </div>
</div>

{% schema %}
{
  "name": "Marquee",
  "settings": [
    { "type": "text", "id": "text", "label": "Text", "default": "Nine Minds — limited drops — luxury streetwear — _nineminds_" }
  ],
  "presets": [{ "name": "Marquee" }]
}
{% endschema %}
